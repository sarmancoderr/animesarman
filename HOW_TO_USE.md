# Submodulos de git

```
git submodule update --remote // Actualziar información de commits de el servidor remoto
git submodule init // Inicializar submodulos
git submodule update // Inicializar carpetas de modulos
```